# Keycloak MCP Server Configuration
#
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the .env file to version control!

# The base URL of your Keycloak server
# Example: http://localhost:8080 or https://keycloak.example.com
KEYCLOAK_URL=

# The client ID for authentication
# This should be a client with "Service Account Enabled" turned on
# Example: admin-cli
CLIENT_ID=

# The client secret for authentication
# You can find this in the Keycloak Admin Console under:
# Clients -> [Your Client] -> Credentials tab
CLIENT_SECRET=

# Optional: The realm to authenticate against (defaults to "master" if not specified)
# REALM=master
